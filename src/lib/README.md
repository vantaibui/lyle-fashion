# Shared technical libraries

This area contains domain-neutral integrations and utilities. Server-only modules must import `server-only`; public environment values must be safe to expose in browser bundles. Do not place business rules here.
