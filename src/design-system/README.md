# Design system structure

CSS custom properties in `tokens/theme.css` are the LYLE Quiet Premium source of truth. `foundations/` contains global element behavior, while reusable implementations live under `src/components`. See `docs/DESIGN-SYSTEM.md` for the approved foundation and its limits.
