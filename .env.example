# Public values are embedded in the browser bundle. Never place secrets here.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only values must not use the NEXT_PUBLIC_ prefix.
# COMMERCE_API_URL=https://api.example.com
# COMMERCE_API_TOKEN=replace-me
