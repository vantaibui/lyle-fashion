# Store locator module

Owns the future list-first store locator integration. Map vendor, geolocation consent, store master data and LocalBusiness schema policy remain unresolved; no runtime implementation belongs here until those contracts are approved.
